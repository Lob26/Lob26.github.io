# LinkedIn OAuth app credentials. Only used locally by scripts/linkedin-auth.mjs
# to mint an access token via the authorization-code flow. NEVER commit real values.
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
